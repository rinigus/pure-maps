Use for imports that are corresponding to Qt 6.x, as much as possible. Ideally, without
any version information. As in the example below:

```
import QtQuick
import QtQuick.Controls
import QtQuick.Layouts
import "."
```
